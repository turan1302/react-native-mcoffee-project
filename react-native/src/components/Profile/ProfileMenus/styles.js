import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container : { marginTop: 10 },

  button_container : {
    backgroundColor: "#fff",
    paddingLeft: 15,
    paddingVertical: 15,
    borderWidth: 0.2,
    borderColor: "#ccc",
    elevation: 5,
    borderRadius: 7,
    marginBottom: 10,
  },
  button_text : { color: "#000", fontSize: 15, fontWeight: "bold" }
});

export default styles;
