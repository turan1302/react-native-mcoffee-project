import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container : {
    backgroundColor: "#fff",
    paddingLeft: 15,
    paddingVertical: 15,
    borderWidth: 0.5,
    borderColor: "#ccc",
    elevation: 5,
    borderRadius: 7,
    marginBottom: 10,
    marginHorizontal : 10
  },

  sss_title : { fontWeight: "bold", color: "#000" },

  description_area : { marginTop: 10, flex: 1 },
  description_text : { color: "#000" },
});

export default styles;
