import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: "center", alignItems: "center" },
  text_area: { marginTop: 20 },
  text_style: { textAlign: "center", fontWeight: "bold", color: "#000" },

  button_style: { marginTop: 20, backgroundColor: "#e15a07", alignItems: "center", padding: 10, borderRadius: 20 },
  button_text : { color: "#fff", fontWeight: "bold" }
});

export default styles;
