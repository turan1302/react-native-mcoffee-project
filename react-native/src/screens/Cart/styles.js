import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container : { backgroundColor: "#fff", flex: 1 },

  flatlist_content_container_style : {
    borderTopWidth: 1,
    borderTopColor: "#ccc",
  },
});

export default styles;
